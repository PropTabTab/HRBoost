@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Reject";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
}

<h1>Reject</h1>

@model List<HRBoost.Entity.Document>
<h2>Kullanıcı Listesi</h2>
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        DataTable Example
    </div>
    <div class="card-body">
        <table id="datatablesSimple">
            <thead>
                <tr>
                    <th>ID</th>
                    <th> Tc</th>
                    <th>DosyaAdı</th>
                    <th>KayıtYeri</th>
                    <th>KayıtTarihi</th>
                </tr>
            </thead>
            <tfoot>
                <tr>
                    <th>ID</th>
                    <th> Tc</th>
                    <th>DosyaAdı</th>
                    <th>KayıtYeri</th>
                    <th>KayıtTarihi</th>
                </tr>
            </tfoot>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <td>@user.Id</td>
                        <td>@user.UserId</td>
                        <td>@user.FileName </td>
                        <td>@user.FilePath</td>
                        <td>@user.UploadDate</td>
                        <th> <a asp-action="Reject" asp-controller="User" asp-area="Admin" asp-route-id="@user.Id" class="btn btn-danger">Sil</a></th>
                    </tr>
                }


            </tbody>
        </table>
    </div>
</div>
